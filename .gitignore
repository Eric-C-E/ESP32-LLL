CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
CMakeUserPresets.json

# ESP-IDF build artifacts and temporary files
build/
build_*/
*.o
*.pyc
*.log

# Project Configuration
# Keep sdkconfig.defaults under version control, ignore the generated sdkconfig
sdkconfig
sdkconfig.old
.config

# Dependency Manager
# Keep dependencies.lock under version control, ignore the managed components
managed_components/
dependencies.lock.old

# IDE/Editor specific files (adjust as needed)
.vscode/
.clangd/
.ccls-cache/
.settings/          # Eclipse settings
.DS_Store           # macOS directory files
*~                  # Emacs temp files
.#*
\#*#

# CLion
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#cmake-build-*
